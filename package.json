{
  "name": "store-analytics-dashboard",
  "private": true,
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "backend",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"bun run --cwd backend start:dev\" \"bun run --cwd frontend dev\"",
    "setup": "bun run db:up && bun run db:migrate && bun run db:seed",
    "db:up": "sh -c 'docker compose up -d postgres redis || docker-compose up -d postgres redis'",
    "db:down": "sh -c 'docker compose down || docker-compose down'",
    "db:migrate": "bun run --cwd backend db:migrate",
    "db:seed": "bun run --cwd backend db:seed",
    "lint": "bun --filter '*' run lint",
    "test": "bun run --cwd backend test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
